import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Sign In - Vantly",
  description: "Sign in to your Vantly salon workspace",
};

export default function LoginPage() {
  return <LoginForm />;
}
