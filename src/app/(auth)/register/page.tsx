import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
  title: "Create Workspace - Vantly",
  description: "Create a new salon workspace on Vantly",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
