import { RegisterForm } from "@/components/auth/register-form";

export const metadata = {
  title: "Create Workspace - Galla",
  description: "Create a new salon workspace on Galla",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
