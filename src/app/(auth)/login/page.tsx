import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Sign In - Galla",
  description: "Sign in to your Galla salon workspace",
};

export default function LoginPage() {
  return <LoginForm />;
}
